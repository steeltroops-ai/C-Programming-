#include<stdio.h>
int main(){
    int x;
    printf("Enter the Number :");
    scanf("%d",&x);
    if (x%2==0,1)
    {
        printf("Given Number can be devided.");
    }
    else
    printf("Given data can not be devide. \n Enter the Number again.");
    return 0;
}